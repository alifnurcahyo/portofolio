// alif.js

document.addEventListener("DOMContentLoaded", function () {
    // Function to handle button click
    function handleButtonClick() {
        alert("Button clicked!"); // Gantilah ini dengan aksi yang sesuai
    }

    // Get the download button element
    var downloadButton = document.querySelector('.tombol-download');
    var downloadButton = document.querySelector('.grup-tombol');
    var downloadButton = document.querySelector('.navigation') ;
    // Add a click event listener to the button
    downloadButton.addEventListener('click', handleButtonClick);
});
